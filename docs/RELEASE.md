# 自动发布指南

🚀 **极简发布流程**：推送tag → 自动部署 → 完成！

## 发布新版本

### 方式1：推送Tag（推荐）

```bash
# 1. 确保代码已提交
git add .
git commit -m "新增xxx功能"

# 2. 创建并推送tag（自动触发部署）
git tag v1.0.1
git push origin v1.0.1
```

完成！GitHub Actions会自动：
- 从tag读取版本号 `v1.0.1`
- 打包应用程序
- 部署到 `https://tools.kyeo.top/`

### 方式2：重新发布现有版本

```bash
# 删除本地和远程tag
git tag -d v1.0.0
git push origin :refs/tags/v1.0.0

# 重新创建并推送
git tag v1.0.0
git push origin v1.0.0
```

## 首次设置

### 1. 配置GitHub仓库

```bash
# 初始化git（如果还没做）
git init

# 运行设置向导
python setup_github_deploy.py

# 按照提示完成GitHub仓库创建和推送
```

### 2. 配置自定义域名

在GitHub仓库：
1. **Settings → Pages → Custom domain**
2. 输入：`tools.kyeo.top`
3. 勾选 **Enforce HTTPS**

### 3. 配置DNS

在你的域名服务商添加CNAME记录：

| 类型 | 主机记录 | 记录值 |
|------|---------|--------|
| CNAME | tools | 你的用户名.github.io |

例如：
```
tools.kyeo.top → kyeo.github.io
```

### 4. 验证部署

推送一个测试tag：

```bash
git tag v0.0.1
git push origin v0.0.1
```

访问查看：
- 下载页面：https://tools.kyeo.top/
- 版本文件：https://tools.kyeo.top/updates/version.json

## 版本号规则

使用语义化版本：`v主版本.次版本.修订号`

```bash
git tag v1.0.0    # 初始版本
git tag v1.0.1    # 修复bug
git tag v1.1.0    # 新增功能
git tag v2.0.0    # 重大更新
```

## 查看部署状态

1. 打开GitHub仓库
2. 点击 **Actions** 标签
3. 查看工作流运行状态

## 常见问题

### Q: 推送tag后没有自动部署？

检查：
1. tag格式是否正确（必须以 `v` 开头，如 `v1.0.0`）
2. GitHub Actions是否已启用
3. 查看Actions日志是否有错误

### Q: 自定义域名不生效？

1. 确保GitHub Pages设置中已添加Custom domain
2. 检查DNS解析是否生效（可能需要几分钟到几小时）
3. 确保勾选了Enforce HTTPS

### Q: 如何回滚版本？

只需重新推送旧版本的tag即可：

```bash
# 删除现有tag
git tag -d v1.0.1
git push origin :refs/tags/v1.0.1

# 重新推送旧版本
git tag v1.0.0
git push origin v1.0.0
```

### Q: 可以手动触发部署吗？

可以，在GitHub仓库：
1. Actions标签页
2. 选择 "Deploy to GitHub Pages"
3. 点击 "Run workflow"

## 自动化流程

```
本地推送 tag v1.0.1
       ↓
GitHub Actions 自动触发
       ↓
从tag提取版本号 "1.0.1"
       ↓
自动更新所有版本文件
       ↓
打包 WorkTools.exe
       ↓
创建更新包 WorkTools_v1.0.1.zip
       ↓
部署到 tools.kyeo.top
       ↓
用户收到更新提示 🎉
```

## 相关地址

| 用途 | 地址 |
|------|------|
| 下载页面 | https://tools.kyeo.top/ |
| 版本检查 | https://tools.kyeo.top/updates/version.json |
| 更新包 | https://tools.kyeo.top/updates/WorkTools_v1.0.1.zip |

## 下一步

配置完成后，用户打开应用程序时会：
1. 自动检查 `https://tools.kyeo.top/updates/version.json`
2. 发现新版本时弹出更新提示
3. 一键下载并安装更新
