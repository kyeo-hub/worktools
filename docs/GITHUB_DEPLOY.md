# GitHub自动部署指南

最简单的自动更新服务器方案：GitHub + GitHub Actions + GitHub Pages

## 特点

- ✅ **完全免费**
- ✅ **自动部署**：每次推送到GitHub自动打包部署
- ✅ **全球CDN加速**
- ✅ **HTTPS支持**
- ✅ **版本管理**

## 快速开始（3步）

### 第1步：运行设置向导

```bash
python setup_github_deploy.py
```

按照提示输入：
- 版本号
- GitHub用户名
- 仓库名

### 第2步：推送到GitHub

```bash
# 初始化git（如果还没做）
git init

# 提交代码
git add .
git commit -m "Initial commit"

# 创建GitHub仓库后，关联并推送
git branch -M main
git remote add origin https://github.com/你的用户名/仓库名.git
git push -u origin main
```

### 第3步：启用GitHub Pages

1. 打开GitHub仓库页面
2. 点击 **Settings** → **Pages**
3. **Source** 选择 **GitHub Actions**

完成！🎉

## 如何使用

### 发布新版本

**方式1：推送代码自动部署**
```bash
git add .
git commit -m "修复bug，优化性能"
git push
```

**方式2：创建版本标签**
```bash
# 修改 version.json 中的版本号
# 然后创建标签
git add .
git commit -m "Release v1.1.0"
git tag v1.1.0
git push origin v1.1.0
```

### 更新日志

编辑 `server/version.json`：

```json
{
  "version": "1.1.0",
  "changelog": [
    "新增图片水印功能",
    "优化定位精度",
    "修复已知bug"
  ]
}
```

## 自动部署流程

```
你推送代码到GitHub
       ↓
GitHub Actions 自动触发
       ↓
打包应用程序 → 生成更新包
       ↓
部署到 GitHub Pages
       ↓
用户收到更新提示
```

## 访问地址

部署完成后，你的更新服务器地址：

```
https://你的用户名.github.io/仓库名/
```

版本文件地址：

```
https://你的用户名.github.io/仓库名/updates/version.json
```

## 配置应用程序

修改 `version.json`：

```json
{
  "version": "1.0.0",
  "update_url": "https://你的用户名.github.io/仓库名/updates/version.json"
}
```

## 常见问题

### Q: GitHub Pages在国内访问慢怎么办？

**方案1：使用Gitee（码云）**
- 在Gitee创建仓库
- 开启Gitee Pages
- 修改部署配置

**方案2：使用CDN**
- 通过 jsDelivr 加速：
```
https://cdn.jsdelivr.net/gh/用户名/仓库名@main/server/version.json
```

### Q: 如何测试是否部署成功？

```bash
# 检查版本文件
curl https://你的用户名.github.io/仓库名/updates/version.json
```

### Q: 私有仓库可以用吗？

可以，但注意：
- GitHub Pages免费版只有公开仓库
- 私有仓库需要升级到Pro或使用其他方案

### Q: 如何回滚版本？

简单！只需修改 `server/version.json` 中的版本号，然后推送。

## 高级配置

### 自定义域名

1. 在仓库根目录创建 `CNAME` 文件
2. 内容为你的域名：`updates.yourdomain.com`
3. 在DNS服务商配置CNAME指向 `你的用户名.github.io`

### 只部署不打包

修改 `.github/workflows/deploy.yml`，注释掉打包步骤：

```yaml
# - name: Build Application
#   run: |
#     python build.py clean
#     python build.py build
```

### 定时自动检查更新

在应用程序中添加定时检查：

```python
# 每天检查一次更新
from PyQt5.QtCore import QTimer

timer = QTimer()
timer.timeout.connect(lambda: auto_updater.check_update(silent=True))
timer.start(24 * 60 * 60 * 1000)  # 24小时
```

## 替代方案对比

| 方案 | 优点 | 缺点 |
|------|------|------|
| GitHub Pages | 免费、自动、稳定 | 国内访问慢 |
| Gitee Pages | 国内访问快 | 需要实名认证 |
| Vercel | 速度快、功能强 | 有使用限制 |
| 自建服务器 | 完全可控 | 需要服务器成本 |

## 需要帮助？

遇到问题请检查：
1. GitHub Actions是否运行成功（仓库Actions标签页）
2. GitHub Pages是否已启用
3. 版本文件路径是否正确
